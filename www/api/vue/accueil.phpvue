<?php
    $_V->composant('entete', $_JETON, [
        NOM_PAGE => 'Accueil',
    ] + $_PARAMS);

    if (isset($_PARAMS[REC_REPONSE]))
        var_dump($_PARAMS[REC_REPONSE]);    

    $_V->composant('pied', $_JETON, $_PARAMS);
?>