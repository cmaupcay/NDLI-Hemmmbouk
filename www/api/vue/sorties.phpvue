<?php
    $_V->composant('entete', $_JETON, [
        NOM_PAGE => 'Sorties en mer'
    ] + $_PARAMS);

    if (isset($_PARAMS[SORTIE]))
        var_dump($_PARAMS[SORTIE]);   

    $_V->composant('pied', $_JETON, $_PARAMS);
?>